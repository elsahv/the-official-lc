---
interface Item {
  title: string;
  body: string;
  href?: string;
  linkLabel?: string;
}

interface Props {
  items: Item[];
  columns?: 2 | 3 | 4;
  minWidth?: number;
  className?: string;
  cardClassName?: string;
}

const {
  items = [],
  columns,
  minWidth = 220,
  className = "",
  cardClassName = "",
} = Astro.props;

const gridCols =
  columns === 2
    ? "grid-cols-1 md:grid-cols-2"
    : columns === 3
      ? "grid-cols-1 md:grid-cols-3"
      : columns === 4
        ? "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"
        : "";

const autoFitStyle = !columns
  ? `grid-template-columns: repeat(auto-fit, minmax(${minWidth}px, 1fr));`
  : "";
---

<section
  class:list={["grid gap-6 bg-", gridCols, className]}
  style={autoFitStyle}
>
  {
    items.map((item) => (
      <article
        id="sq"
        class:list={[
          "border-2  border-black rounded flex min-h-[220px] flex-col justify-between  bg- p-5 ",
          cardClassName,
        ]}
      >
        <div class="">
          <h3 class="mb  bg- font-bold leading-tight text-xl">{item.title}</h3>

          <p class="bg- pl-2 text-md leading-relaxed ">{item.body}</p>
        </div>
        <button class="">
          {item.href && (
            <a
              href={item.href}
              class="bg- mt-4 inline-block text-sm underline underline-offset-2 opacity-80 pl-3"
            >
              {item.linkLabel || "Learn more"}
            </a>
          )}
        </button>
      </article>
    ))
  }
</section>
